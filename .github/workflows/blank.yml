name: Mi primer workflow

on:
  push:
    branches:
      - main

jobs:
  saludo:
    runs-on: ubuntu-latest

    steps:
      - name: Decir hola
        run: echo "Hola, GitHub Actions está funcionando 🚀"
